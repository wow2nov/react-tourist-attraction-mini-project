import { useState ,useEffect } from "react";

const API_BASE = "http://localhost:4001";

export default function FetchTripsDemo () {
    const [trips, setTrips] = useState([]);

    const [input, set] = ([])
}